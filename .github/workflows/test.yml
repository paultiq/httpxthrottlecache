name: Test and Coverage

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

concurrency:
  # cancels superceded runs
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  LATEST_PY_VERSION: &latest '3.14'
  EDGAR_IDENTITY: "Iq De de_iq@iqmo.com"

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: true
      matrix:
        include:
        # test oldest & latest versions against ubuntu
        # and one version against windows and macos
        # full matrix runs the recommended httpx2 extra;
        # the httpx extra is tested at the oldest & latest python versions
        - { os: ubuntu-latest, python-version: "3.10", http-extra: httpx2 }
        - { os: ubuntu-latest, python-version: "3.10", http-extra: httpx }
        - { os: ubuntu-latest, python-version: *latest, http-extra: httpx2 }
        - { os: ubuntu-latest, python-version: "3.14t", http-extra: httpx2 }
        - { os: ubuntu-latest, python-version: "3.15", http-extra: httpx2 }
        - { os: ubuntu-latest, python-version: "pypy3.11", http-extra: httpx2 }
        - { os: windows-latest, python-version: *latest, http-extra: httpx2 }
        - { os: macos-latest, python-version: *latest, http-extra: httpx2 }
    environment: test
    permissions:
      contents: write
    steps:
      - name: Check out code
        uses: actions/checkout@v7.0.1
        with:
          fetch-depth: 0
      - uses: actions/setup-python@@v7.0.0
        with:
          python-version: ${{ matrix.python-version }}
          allow-prereleases: true
      - name: Install uv
        uses: astral-sh/setup-uv@v10.0.0
        with:
          enable-cache: true
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: uv sync --extra ${{ matrix.http-extra }}
      - name: Run tests with coverage and Build
        env:
          COVERAGE_PROCESS_START: ${{ github.workspace }}/.coveragerc
        run: |
          uv run pytest --verbose --cov --cov-report=term-missing --ignore=tests/extras
          uv build
      - name: Smoke Test Extras - Edgar and HTTP2
        if: ${{ matrix.python-version == env.LATEST_PY_VERSION && matrix.os == 'ubuntu-latest' && matrix.http-extra == 'httpx2' }}
        run: |
          uv sync --extra ${{ matrix.http-extra }} --group edgartest
          uv run pytest tests/extras/test_edgartools.py tests/extras/test_http2.py
      - name: Test Proxy
        if: ${{ matrix.python-version == env.LATEST_PY_VERSION && matrix.os == 'ubuntu-latest' && matrix.http-extra == 'httpx2' }}
        run: |
          docker run -d --name tinyproxy -p 6666:8888 -e ALLOW=0.0.0.0/0 monokal/tinyproxy:latest ANY
          export HTTP_PROXY=http://127.0.0.1:6666
          export HTTPS_PROXY=http://127.0.0.1:6666
          curl -s -o /dev/null -w "%{http_code}\n" http://httpbingo.org/get  | grep -q '^200$'
          curl -s -o /dev/null -w "%{http_code}\n" https://httpbingo.org/get | grep -q '^200$'
          uv run pytest tests/extras/test_proxy.py tests/extras/test_http2.py

      - name: Python Coverage Comment if main
        if: ${{ github.ref == 'refs/heads/main' && matrix.python-version == env.LATEST_PY_VERSION && matrix.os == 'ubuntu-latest' && matrix.http-extra == 'httpx2' }}
        uses: py-cov-action/python-coverage-comment-action@v3.23
        with:
          GITHUB_TOKEN: ${{ github.token }}